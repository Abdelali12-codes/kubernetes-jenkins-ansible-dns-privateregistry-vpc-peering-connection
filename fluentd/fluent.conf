<source>
 @type http 
 bind 0.0.0.0
 port 9880
</source>
# <match access.log>
#   @type stdout
# </match>
<match access.log>
  @type elasticsearch
  host localhost
  port 9200
  logstash_format true
  index_name fluentd.${tag}
</match>